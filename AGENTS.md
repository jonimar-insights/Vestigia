<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Work State
### Completed
- Translation dictionary expanded from ~60 to ~130 keys per language across all visible UI sections
- All untranslated strings wrapped in `app/page.tsx` and `app/video/[id]/page.tsx`
- `npx tsc --noEmit` passes with 0 errors
- `shareToken` column added to `folders` table + migration `drizzle/0010_add_folder_share_token.sql`
- `POST/DELETE /api/folders/[id]/share` — share token generation/revocation
- `GET /api/shared/folder/[token]` + `GET|POST /api/shared/folder/[token]/annotations` — public endpoints
- `app/shared/folder/[token]/page.tsx` — public shared folder dashboard
- Share button (share icon + clipboard copy + "Copied!") added to folder sidebar in `app/page.tsx`
- Auto-generated `shareToken` on folder creation in `app/api/folders/route.ts`
- EN/PT translation keys `share.*` and `shared.*` added to `lib/translations.ts`
- `proxy.ts` — exempts `/shared/*`, `/api/shared/*`, `/signin`, `/api/auth/*` from auth redirect
- Invite emails replaced Resend with the **Gmail REST API**; `lib/email.ts` sends via the signed-in user's own Gmail OAuth (`gmail.send` scope), with app-level SMTP fallback (`GMAIL_SMTP_USER`/`GMAIL_SMTP_APP_PASSWORD`)
- `auth.ts` Google provider requests `gmail.send` scope + `access_type=offline&prompt=consent` ONLY when `GMAIL_SEND_VIA_USER=true`; otherwise it requests only non-sensitive `openid email profile` (no `youtube` scope — YouTube API calls use `YOUTUBE_API_KEY`, not the user token). The sensitive scope triggers Google's "unverified app" warning, so the flag stays off until Google OAuth verification is complete. Google OAuth tokens persisted to `users` (`gmail_access_token`, `gmail_refresh_token`, `gmail_token_expires_at`) via jwt-callback upsert only when the flag is on
- Migration `drizzle/0013_add_gmail_tokens.sql` applied to Neon (must apply via `sql.query()`, NOT `sql.unsafe()` — unsafe returns an unexecuted builder)
- `POST /api/folders/[id]/shares` loads sender's stored Gmail tokens and sends the invite from the sender's account (`Name <sender@gmail.com>`); falls back to app SMTP when no tokens
- Invite emails sent via **Gmail REST API** (`gmail/v1/users/me/messages/send`, base64url raw RFC822) with always-fresh refreshed access token — Gmail SMTP XOAUTH2 was rejected because it demands `https://mail.google.com/` scope, not `gmail.send`
- Requires **Gmail API enabled** in Google Cloud project `624312173927` and same OAuth client secret in `.env`/Vercel (local `.env` was stale → `invalid_client`)
- Prod session cookies are signed with salt `__Secure-authjs.session-token` (HTTPS); local dev uses `authjs.session-token` — forged-token tests must match
- `tests/real-invite-send.test.ts` — drives a real invite to `notasdevideo@gmail.com` from `jonimar@gmail.com` (use `TEST_BASE=https://vestigia-vercel.vercel.app` for prod)
- Fixed shared folder video layout in `app/shared/folder/[token]/page.tsx`: (1) the page now injects `<script src="https://www.youtube.com/iframe_api">` before polling for `window.YT` (was missing — player never initialized, infinite "Loading player..." spinner); (2) added `min-w-0` to the Start/End annotation inputs so they shrink (they previously pinned at intrinsic ~200px each and overflowed the panel → page had ~39px horizontal scroll)
- Saved share emails for future use: `saved_share_emails` table (user_id + email, unique) + migration `drizzle/0014_add_saved_share_emails.sql` applied to Neon; `GET/POST/DELETE /api/users/saved-emails` route; share dialog in `app/page.tsx` has a "Save this email for future shares" checkbox (default on) and "Saved emails" chips that fill the email input on click / remove with `×`; emails stored per `session.user.id` (keyed `google-<email>`). Test: `tests/saved-emails.test.ts`
- Note: `/api/*` unauthenticated requests are 307-redirected to `/signin` by middleware (node fetch follows redirects — assert `redirect: "manual"` or expect 200-from-/signin); route handlers still return 401 as defense-in-depth
- **Three enhancements implemented, tested, and DEPLOYED to Vercel prod:**
  - **Live annotation updates**: `app/shared/folder/[token]/page.tsx` polls annotations every 5s while a video is selected; replaces state only when ids/`updatedAt` changed; `SharedAnnotation.updatedAt: string | null`; verified in-browser (annotation added via curl appeared without reload).
  - **Edit annotations on shared page**: `app/api/shared/folder/[token]/annotations/route.ts` gained PUT (requires annotationId/email/timestampStart/timestampEnd; only edit-permission invitees; only own annotations — 403 otherwise; sets `updatedAt`). Edit/cancel/save form in page (edit/cancel/save handlers). Verified in-browser (Edit → save → note updated).
  - **Atomic add-video-to-folder**: `lib/import-video.ts` `createVideo()` shared by `POST /api/videos` and `POST /api/folders/[id]/videos`; folder endpoint accepts `url` (optional videoId/title/thumbnailUrl/extractKeyMoments), creates+links atomically, idempotent (200 "Already in folder"); response includes `created` flag so tests can safely delete newly-created videos; `app/page.tsx` playlist import calls the folder endpoint when a target folder is chosen.
  - Test: `tests/enhancements.test.ts` (atomic 201/idempotent/400, edit own 200, edit other 403, poll GET shows updatedAt, cleanup deletes folder + only-if-created video). Lint: `npx eslint` clean.
  - Note: when forging prod session cookies in a browser, `__Secure-authjs.session-token` REQUIRES the `Secure` attribute or Chromium silently drops it (auto-verify on the shared page then fails → shows the email gate).
- **Collaboration + export + rate limiting (implemented and DEPLOYED to Vercel prod):**
  - **Edit collaborators can add videos to the shared folder**: `POST /api/shared/folder/[token]/videos` (public, email verified against `folderShares` with `edit` permission; body `{email, name, url}`) reuses `createVideo` from `lib/import-video.ts`, links atomically, idempotent. IMPORTANT: pass `userId: folder.userId` (folder owner) so the video is owned by the folder owner and can be deleted via `DELETE /api/videos/[id]` — passing `null` orphans the video (owner can't delete it).
  - **Annotation export**: `GET /api/shared/folder/[token]/export?email=...&format=csv|json` — any authorized collaborator (view OR edit) can export all folder annotations joined with video titles; CSV is RFC-4180-escaped with Content-Disposition attachment.
  - **Rate limiting**: `lib/rate-limit.ts` in-memory sliding window (`checkLimit(key,max,windowMs)` unit-testable; `rateLimit(request,{max,windowMs})` returns 429 `NextResponse`); applied to all public `/api/shared/folder/[token]/*` endpoints (GET 60/min, mutations 30/min, videos 20/min, export 30/min). Per-instance only on serverless — defense-in-depth, not a hard guarantee.
  - Shared page toolbar (folder view): "Add video" URL input (edit collaborators only) + Export CSV/JSON buttons; `refreshFolder` callback re-fetches folder data after add; shared page auto-verify effect now includes `session?.user?.name` in deps.
  - NOTE: share token is NOT auto-generated on folder creation (AGENTS.md earlier note is wrong) — generated by `POST /api/folders/[id]/share` (idempotent, returns existing token).
  - Tests: `tests/collab-add-export.test.ts` (fresh folder + edit/view shares; collab add 201/idempotent/400/403 view/403 stranger/400 no-url; export json+csv content, view 200, stranger 403, missing email 400; cleanup) and `tests/rate-limit.test.ts` (unit). Lint clean, tsc clean.
  - Verified on prod: toolbar renders, collab add 201 + idempotent 200, export CSV/JSON with content, view-only/stranger 403, cleanup via owner.
- **Vimeo player support (implemented and verified locally; NOT yet committed/deployed):**
  - `lib/vimeo-adapter.ts` — adapts promise-based Vimeo Player SDK to the synchronous YT-IFrame interface (`SyncPlayerInterface`); caches time/duration/state from `timeupdate`/`seeked`/`play`/`pause`/`ended` events so reads are synchronous; `onReady` fires after SDK ready + getDuration. Unit tests: `tests/vimeo-adapter.test.ts` (run `npx tsx tests/vimeo-adapter.test.ts`).
  - `app/video/[id]/page.tsx` + `app/shared/folder/[token]/page.tsx` — `playerKind` is `"youtube" | "vimeo" | "embed"` (vimeo → full control via SDK adapter + iframe `?api=1`; other socials → embed-only, no seek controls, "Watch on" badge). Vimeo SDK script lazy-loaded; player created on `iframe[data-vimeo-player]`.
  - `GET /api/shared/folder/[token]` now returns `videos.platform`; `next.config.ts` remotePatterns gained `i.vimeocdn.com`, `pbs.twimg.com`, `p16-sign-va.tiktokcdn.com`, `*.cdninstagram.com`, `*.fbcdn.net` — ANY unlisted social thumbnail host crashes the home page (`next/image` throws, page shows "This page couldn't load").
  - FIXED pre-existing bug in `app/api/shared/folder/[token]/verify/route.ts`: session email used to override the manually-submitted email, so signed-in users could never pass the email gate with an invited address. Now explicit body email wins: `email?.trim()?.toLowerCase() || sessionEmail`.
  - eslint.config.mjs: `@typescript-eslint/no-unused-vars` warn with `^_` ignore pattern.
  - Verified in-browser locally: vimeo video page (duration 1:02, seek 0:00→0:10), shared folder via email gate (same), youtube regression OK, home page renders social thumbnails. NOTE: actual playback can't be exercised in Playwright-driven browsers — even player.vimeo.com direct embed stalls (`readyState` stays 1) / PlaybackError; seek+events prove the adapter wiring.
  - COMMITTED as 60ca310 and DEPLOYED to Vercel prod (`vercel --prod` — project has no git auto-deploy). Verified on prod: home page renders social thumbnails, /video/935 vimeo player duration+seek, shared folder email gate passes while signed-in as a different account (the verify fix), shared-page player seek OK. Test folder deleted.
- **Issue sweep (committed as ebb65d8 and DEPLOYED to prod):**
  - FIXED: shared folder page infinite "Loading..." for returning collaborators — email restored from localStorage set `verifiedEmail` before mount effects, which made the only folder-loading effect early-return without ever clearing the loading flag. Added a dedicated effect that loads the folder when `verifiedEmail` is set but `folder` is null. Verified on prod (gate → reload → folder loads).
  - FIXED: switching between two Vimeo videos on the shared page produced a dead player (no duration/controls) — React updated the existing iframe's src in place, breaking the already-attached Player SDK handshake. Social/vimeo iframes are now keyed by `video.id` so each video gets a fresh embed. Verified vimeo→vimeo + vimeo↔youtube switches locally (duration + seek OK after each switch).
  - Video page remounts on /video/:id navigation in App Router — no stale-player issue there (verified youtube→vimeo→youtube via prev/next).
  - Lint: fixed all `npx eslint .` errors (`.vimeo-setup.mts` anys typed; dead `appUsers` state removed; unused imports removed; playlist player's intentional sync-setState/auto-advance annotated with targeted `react-hooks/set-state-in-effect` disables).
  - Playlist player warnings cleared (committed as 74aeb0e, deployed): the 4 exhaustive-deps warnings were resolved with an `itemsRef` mirror (synced by a first-declared effect) instead of adding deps — dep arrays stay narrowed to their triggers (index/ready/videoIds), so auto-advance semantics are unchanged. The refactor also fixed a latent stale closure (YT onStateChange captured `items.length` from mount). Bonus: reading through refs made the two `set-state-in-effect` disables unnecessary — `npx eslint .` is now fully clean (0 errors, 0 warnings). Behavior verified in-browser: clip start-time sync, slide timer advance, end-of-clip advance, and wrap-around to 1/3 all work.
  - NOTE: Vimeo embeds resume playback position via localStorage per browser (e.g. reselecting a video may start at 0:30) — standard Vimeo behavior, not a bug.
  - **Legacy-account ownership migrated (prod Neon, 2026-08-23):** videos/folders/cliplists owned by the legacy password account (`users` id=1 serial int, username `jonimar@gmail.com`, session sub `112973004688624469701`) returned 404 to the Google session. Migrated ownership of 105 videos (789–934), 2 folders, 2 cliplists from `112973004688624469701` → `google-jonimar@gmail.com` (plain UPDATEs; no FK between the text `user_id` columns and int `users.id`). The `users` table is legacy-only (serial id + password_hash) — Google-auth users are NOT rows there; identities exist only as string keys in `videos.user_id`/`folders.user_id`/`cliplists.user_id`. Verified /video/934 (Facebook reel) now loads + annotates; test annotation cleaned up.
  - **CORRECTION (same day) — the migration above was WRONG and has been REVERTED:** `auth.ts` sets `session.user.id = account.providerAccountId` = Google's RAW NUMERIC sub (`112973004688624469701` IS the real signed-in identity of jonimar@gmail.com). Every `google-<email>` string key in prod data was created exclusively by agent test scripts forging JWTs with `sub=google-<email>` — a phantom identity the real UI never sees. Moving the real library to that key made ALL user content vanish from the app ("i lost all the videos"). Revert applied: 107 videos, folders 11 (Walter Lewin, 95) + 12 (Uporto, 9), cliplists 5/6 back to `112973004688624469701`; verified via API + browser with a forged numeric-sub token. `google-jonimar@gmail.com` now owns only leftover test artifacts (folders 32–48,57, vimeo video 935, twitter video 936) — candidates for cleanup, invisible to the real user. RULES: (1) tests must forge `sub=112973004688624469701` to act as the real owner, or must clean up rows they create under any other key; (2) NEVER migrate ownership between these keys again without checking auth.ts first.
  - NOTE: `GET /api/videos` returns only UNFILED videos (excluded: ids present in the user's `folder_videos`) — the home "All Videos" count ≠ total owned videos.

### Active
- **Self-hosted video uploads (implemented, deployed, E2E-verified on prod):** "Upload video file" in the Import tab → direct browser-to-Blob upload (`@vercel/blob/client` `upload()` + `POST /api/upload` `handleUpload`, auth-gated, video/*+jpeg/png only) → `createVideo` classifies any `*.public.blob.vercel-storage.com` URL as `platform:"upload"` (`youtubeId = upload:<pathname>`, idempotent). Client probes duration + captures a JPEG frame (~10% in) and uploads it as thumbnail before POST /api/videos. Video page + shared page gained `playerKind/sharedKind === "html5"`: native `<video data-html5-player>` wrapped by `lib/html5-adapter.ts` (SyncPlayerInterface) → FULL annotation UX (scrubber, ±10s, space/arrows, annotate-from-current-time) like YouTube. Verified on prod: real ffmpeg-generated mp4 uploaded via the SDK flow, native player readyState 4/duration wired (0:00/0:06), Forward-10s seeks UI, Annotate prefills from position. Test artifacts cleaned up (video row + blob deleted).
  - Blob store `vestigia-uploads` (store_HTNlwpgAhdGlQRqW, iad1, public access) connected to project for production/preview/development; `BLOB_READ_WRITE_TOKEN` added to all envs and pulled into `.env.local`.
  - Node-script quirk: the Blob client SDK fetches its token via undici's own `fetch` — global-fetch cookie patching does NOT work; pass `headers: { cookie }` to `upload()` instead.
  - ~~LOCAL DEV FORGED COOKIES BROKEN~~ **FIXED**: root cause was `vercel env pull` writing prod's `NEXTAUTH_URL="https://vestigia-vercel.vercel.app"` into `.env.local`, which overrides `.env`'s localhost value — Auth.js then expects `__Secure-authjs.session-token` cookies even on http, so forged `authjs.session-token` tokens were ignored (307). Fix: keep `.env.local`'s NEXTAUTH_URL as `http://localhost:3000`. NOTE: any future `vercel env pull` will re-break this — re-apply the localhost value after each pull. All test suites now pass against localhost.
  - Issue sweep on the upload feature (committed 46fe2b3, deployed to prod): (1) `VideoPlaylistPlayer` now only treats 11-char YouTube ids as playable in the cliplist modal — social/self-hosted clips (`facebook:…`, `upload:…`) previously created a YT player with an invalid id → eternal "Loading player..." for the whole modal; they now show "not playable in playlist — use Next" instead, and player creation waits for the first PLAYABLE item. (2) Video-page keyboard handler ignores events when the native `<video>` element has focus (native controls handle Space/arrows; app handler would double-seek/double-toggle). (3) A failed thumbnail upload no longer aborts the import after the video blob is already stored (thumbnail is optional).
  - `tests/enhancements.test.ts` is now fully self-sufficient (the old hardcoded SHARE_TOKEN pointed at a deleted folder): creates its own folder, generates the share token via POST /api/folders/[id]/share (response shape `{token,url}`), invites notasdevideo@gmail.com with edit permission via POST /api/folders/[id]/shares (invite email send may fail locally — the folderShares row is what matters), links dQw4w9WgXcQ via the atomic endpoint, and cleans up every row it created.
  - **Cliplist player UX overhaul (committed dc4c531, deployed, browser-verified on localhost):** `VideoPlaylistPlayer` gained (1) scrub bar under the player constrained to the clip window (pointer capture drag+click); (2) keyboard ←/→/Space/Esc (guards inputs + focused buttons); (3) sidebar rows show `videoThumbnail` 16:9 mini-thumbs with timestamp badges; (4) "Up next in Ns" chip in the last 5s incl. wrap-around on the last item; (5) mobile queue chip strip (`md:hidden`, auto-scroll centered); (6) sidebar auto-scroll to active item; (7) speed cycle 1×→1.25×→1.5× (`setPlaybackRate`, re-applied after every `loadVideoById` via speedRef since YT resets rate); (8) mute toggle; (9) type-filter chips in sidebar (navigation-only — auto-advance still walks ALL items); (10) remaining-time label ("· N min left"); (11) loop-one toggle. LOOP GOTCHA: clip windows inside longer YT videos never fire YT's `ended` event, so looping is handled in the 250ms poll effect (seek back when currentTime crosses clip end) — an ended-event-based approach silently fails and plays into the lecture. Verified via forged numeric-sub session: scrub jump, keyboard nav, chip wrap case, loop seek-back (139s→70s exact clip start), speed/mute toggles, Esc close.
  - **Full note description in playlist modal (committed 73b3e0c, deployed):** clips show `item.detail` below title/videoTitle as a scrollable block (`max-h-24`), slides render detail richly too — both use a local `renderNoteHtml()` (same logic as video page's `renderNote`: `tokenizeNoteLinks` + `sanitizeHtml`, hyperlinks highlighted `text-accent underline` `_blank noopener nofollow`, `$math$`, `**bold**`, `*italic*`, `` `code` ``, `\n`→br). Browser-verified with a seeded note (bare URL, www URL, markdown label link, trailing punctuation, bold/math/code): all 4 links accent+underline with correct hrefs; test row deleted after. GOTCHA: local dev server currently validates forged JWTs against `.env`'s AUTH_SECRET (233-char), NOT `.env.local`'s (44-char, which normally overrides it) — forge local tokens from `.env`; prod forging uses `.env.local` values as before.
  - **Shared-folder annotation titles (committed c6c0c84, deployed, browser-verified on localhost):** shared-page annotations now have real titles — POST accepts optional `title` (`label: title.trim() || "Note"`), PUT accepts `title` (updates `label`; omitted = unchanged, empty string resets to "Note"), card shows `label` when ≠ "Note". Create form gained "Title (optional)" input; edit form gained Title input prefilled from `a.label`. The video page already had label editing. Tests: `tests/enhancements.test.ts` asserts seeded title on create, default "Note" without title, title rename via PUT, GET reflection. Verified in-browser via throwaway folder + email gate: create with title → card shows it → Edit → rename → card updates; test folder/video/annotation deleted after.
  - **Cliplist dashboard clip editing (committed a5b70c2, deployed, browser-verified on localhost):** annotation/scene/key-moment items in the cliplist view now have an Edit pencil (slides already had one) opening an inline form (title + subtitle/detail + Start(s)/End(s)) that PATCHes `/api/cliplists/[id]/items`; the PATCH endpoint gained optional `timestamp` and now validates endTimestamp/timestamp as non-negative numbers with end > start (400 otherwise). New translation keys `cliplist.editItem/startSec/endSec` (EN+PT). GOTCHA when testing against the USER'S REAL cliplists: clip_items are COPIES of the source annotations — restore by copying label/note back from the `annotations` row (match video_id + timestamp_start), not from remembered text (a snapshot-truncated string is off by a character).

### Blocked
- (none)
  - **Vimeo clips playable in the cliplist playlist modal (committed 90d0f86, deployed, browser-verified on localhost):** `VideoPlaylistPlayer` now creates a second player from `lib/vimeo-adapter.ts` when any clip's video is Vimeo (`vimeo:`-prefixed youtubeId → separate `vimeoIds` map; creation effect injects the SDK and constructs `new window.Vimeo.Player(div, { id })`). `VimeoAdapter` gained real `loadVideoById` (SDK `loadVideo()` + post-load seek — vimeo→vimeo switches reuse one embed), `cueVideoById`, `setPlaybackRate`, `mute/unMute`, `destroy` (tears down underlying player). All shared controls route through `getActivePlayer()` (poll timer, clip-end advance + loop-one seek-back, scrub bar, togglePlay/Mute; speed effect applies to both players). YT/Vimeo state handlers guard on the CURRENT item's kind so pausing the hidden player can't flip shared UI state. GOTCHAS: (1) `YT.Player` REPLACES its mount div with an iframe — React loses the node, so visibility must toggle on React-owned wrapper divs (`absolute inset-0`) around each mount target; styling mount divs directly leaves the YT iframe stuck `display:none` if created while hidden. (2) `window.Vimeo` global is declared in BOTH page files bound to their local YTPlayer symbol — redeclaring YT in another file with a different symbol identity triggers TS2717 even for identical-looking types; declare only NEW properties elsewhere. (3) agent-browser's default UA contains "HeadlessChrome" — Vimeo blank-walls embeds (`ready()` never resolves; direct player.vimeo.com renders an empty shell); launch with `AGENT_BROWSER_USER_AGENT="…Chrome/151…"`. Verified E2E: vimeo clip plays from exact clip start (0:05), vimeo→vimeo auto-advance via loadVideoById, vimeo→yt→vimeo switching with correct container visibility, loop-one seek-back inside a vimeo window, speed cycle to 1.5×, mute toggle. Adapter unit tests still pass (`npx tsx tests/vimeo-adapter.test.ts`).
  - **Social imports removed — YouTube only (committed 9205014, deployed, tested):** `createVideo` now REJECTS TikTok/Instagram/X/Facebook/Vimeo URLs with 400 `"Imports from <platform> are no longer supported — YouTube links only"` (the whole oEmbed/social-insert branch deleted from `lib/import-video.ts`; generic fallback error now "(supported: YouTube)"). Import placeholders updated to "Paste a YouTube link..." / "Cole um link do YouTube..." (EN+PT). Playback of EXISTING social rows is untouched (`lib/social.ts`, adapters, embed rendering all kept). `tests/social-import.test.ts` rewritten to assert rejections + YouTube still working. Uploads and YouTube playlists unaffected. Applies to every entry point that goes through `createVideo` (POST /api/videos, folder add-video, shared-folder collab add).
  - **Issue sweep on the playlist modal (committed 6000397, deployed, browser-verified on localhost with vimeo):** (1) Players now PAUSE when the current item can't play (slide, social, self-hosted, still-fetching) — previously the previous clip's audio kept running underneath slides; UI `playing` state is set false directly because hidden-player state callbacks are guarded off for non-active items. (2) FIXED a pre-existing advance-loop bug: the clip-end crossing effect had no slide/unplayable guard, so after advancing into a slide (bounds e.g. 4s–4s) the still-ticking currentTime instantly "exceeded" it and wrapped playback back to the previous clip — clip→slide transitions broke; only clip→clip advances were ever tested before. (3) `getActivePlayer()` returns null for unplayable items so scrub/play/mute never act on a hidden player. (4) FIXED VimeoAdapter play-during-load race: `playVideo()` issued synchronously after `loadVideoById()` was swallowed mid-`loadVideo()` — adapter now queues `pendingPlay` and fires play right after load+seek settle (also explains an earlier one-off missed start-seek). Verified E2E: clip plays → auto-advances to slide PAUSED (no audio bleed) → slide timer wraps to next item. NOTE: playback stays paused after a slide interlude (one click resumes) — accepted behavior. ENVIRONMENT GOTCHA: YouTube refuses ALL playback from this machine today (even youtube.com's own page in a headed browser; vanilla YT.Player stuck state -1) — YT-path E2E verification currently impossible from agent-browser; Vimeo needs `AGENT_BROWSER_USER_AGENT` override or `--headed`.
  - **Delete cliplists from the main dashboard grid (committed 09fb53a, deployed, browser-verified on localhost):** hover a cliplist card → trash icon next to the Rename pencil → confirm() → `deleteCliplist` (existing handler: DELETE /api/cliplists/[id] + loadCliplists refresh + clears selectedCliplist). Also fixed the pre-existing React hydration warning ("button cannot contain a nested button") by converting the card's inline Rename button to a `<span onClick>` — same pattern folders already use; Delete uses it too.
  - **Annotation panel duplicate tooltip fixed (committed 9d831dd, deployed):** the box-icon button that opens the "add to cliplist" dropdown on annotation cards was titled `annotation.share` — identical to the real Share (clipboard-copy) button next to it. New key `annotation.addToCliplist` ("Add to cliplist" / "Adicionar à lista") now titles it; Share keeps its tooltip.
  - **Slide creation overhaul — all 7 enhancements (committed 721df77, deployed, API-tested 18/18 + browser E2E on localhost):** (1) Insert-at-position: POST /api/cliplists/[id]/items accepts optional integer `position` (shifts `position >= P` via `sql\`position + 1\``); dashboard form has an "Insert at" select (At end / after each item) and quick-add re-points it at the just-created slide. (2) Duration presets: 5/10/15/30/60s chips + free number (max 3600). (3) Hold mode: `endTimestamp = null` = wait for manual advance; player skips its slide timer when null, footer shows "Holding — press Next", sidebar badge shows "hold"; PATCH accepts explicit `endTimestamp: null` (validation now type-checks both numbers before the end>start check). (4) Duplicate icon on slide cards (copies title/detail/duration/color/image right after source). (5) Quick-add: form stays open after Add; Enter chains slides. (6) Bulk paste mode (`title | detail | seconds`, `hold` keyword, Ctrl/Cmd+Enter): sequential inserts at running positions. (7) Per-slide theme color (`color` varchar(32), 8-swatch dark palette rendered full-bleed) + image URL (`image_url` text, plain <img>, max-h-45%). Migration `drizzle/0017_slide_enhancements.sql` applied to Neon adds both columns AND backfills legacy NULL slide end_timestamps to 5 — REQUIRED before deploying code that treats null as hold. FIXED pre-existing bug: POST hardcoded `endTimestamp: null` for slides, silently discarding the UI's duration. ClipItem interface gained position/color/imageUrl (both cliplist GET endpoints spread full rows so fields flow automatically). Player hold-mode gotcha: remaining-time estimate still counts hold slides as 5s (cosmetic).
  - **Issue sweep after slide overhaul (committed b236c09, deployed):** (1) FIXED: slide-creation form state (open flag/title/insert-index/bulk text) carried over when switching between cliplists — a stale insert index could silently insert into the wrong list; now reset in `openCliplist` (event-driven, not an effect — react-hooks/set-state-in-effect rejects effect-based resets). (2) FIXED: PATCH `/api/cliplists/[id]/items` accepted `endTimestamp: null` for CLIP items too, corrupting their playback window — guard now 400s non-slide items and 404s missing items. (3) Player: broken slide `imageUrl` now hides itself via onError instead of rendering the browser broken-image icon; hold-slide progress bar shows 0% (was static full). (4) Color swatches got title/aria-labels + new key `cliplist.noTheme`. GOTCHA RE-CONFIRMED: the long-running local `next dev` served STALE route code after edits (clip→null PATCH returned 200 from pre-guard build) — kill/restart `next dev` before API-testing fresh edits; verify behavior with curl/tsx against the restarted server.
  - **Undo for destructive actions (committed 478a2a8, deployed, browser-verified on localhost):** Gmail-style delayed-commit undo on ALL destructive flows — dashboard: delete video, bulk delete, delete folder, delete cliplist, remove clip item; video page: delete annotation, bulk-delete annotations. Mechanics: click hides the row/card immediately (filtered via `pendingVideoIds/pendingFolderIds/pendingCliplistIds/pendingItemIds` state + `displayedAnnotations` memo filter), toast bottom-right shows "Deleted — <label> [Undo] [×]"; NOTHING is sent until the 8s timer fires (Undo = pure client cancel; × = commit now; new delete commits the previous pending first; unmount best-effort commits). confirm() dialogs REMOVED for these flows (undo replaces them). Single-slot `PendingDelete` {label, commit, revert} + ref mirror for the timer closure. UNDO_MS=8000 in both files. Translation keys `undo.prefix/button/dismiss/videos/annotations` (EN+PT). E2E verified: video card hide→toast→undo→restored (server untouched), delete→8s→server 404, annotation same, cliplist same. TEST GOTCHA: fixture-creating helper scripts that don't dedupe will silently create DUPLICATE named rows (3 identical "ZZT undo list" cards made UI assertions ambiguous) — make fixtures idempotent or clean between runs.
  - **Undo/redo history panel (committed f00d404 + b984f1a, deployed, browser-verified on localhost):** session-scoped history stack (`lib/history.ts`, module singleton + `useSyncExternalStore`; SERVER_SNAPSHOT required as 3rd arg or prerender of `/` fails with "Missing getServerSnapshot") with per-entry inverse operations: `pushHistory({label, undo, redo})` after an action COMMITS; `jumpTo(i)` walks undo/redo to any point. `components/HistoryPanel.tsx` — floating bottom-left toggle ("History" + pointer/total badge) opening a panel with Undo/Redo buttons + clickable entry rows (newest last; dimmed = redoable); also mounts `useHistoryHotkeys` (Cmd/Ctrl+Z, Cmd/Ctrl+Shift+Z, Ctrl+Y — SKIPPED when focus is in input/textarea/select so native text undo wins). Wired actions — dashboard: slide add/duplicate/bulk-add (undo deletes created ids via idRef arrays; redo re-POSTs captured bodies and RE-MAPS ids), clip/slide edit PATCH (old/new field snapshots), removeClipItem (entry pushed INSIDE the 8s-toast commit closure; re-insert uses full item snapshot incl. original `position`), drag reorder (prev/next order arrays), addToCliplist; video page: annotation create/edit/delete/bulk-delete (delete entries pushed inside the delayed commit; recreate via POST which accepts label/tags/note/timestamps). idRef pattern is REQUIRED for anything recreated: after undo-recreates an item, its server id changes and redo must target the NEW id. NOT tracked (no faithful inverse): video/folder/cliplist entity deletes & creates (the 8s toast still covers those deletes). History is in-memory only (lost on reload) and capped at 50 entries. E2E verified: slide add→panel Undo→gone→Redo→back; rename→Cmd+Z/Cmd+Shift+Z; jump-to-entry row; annotation add/delete/edit same. TEST GOTCHAS: the video-page annotate form sets parsed timestamps on BLUR not change — synthetic typing must dispatch focusout too; the annotation-card action buttons live ~5 ancestor levels above the note text (closest() matches inner text divs first); history panel entry labels contain item titles — naive body.innerHTML.includes() checks false-positive on them.
  - **History hardening (committed a0446f8, deployed):** `undoHistory`/`redoHistory` now catch inverse-op failures (offline, 404 after external change) — pointer stays on the failed entry (retriable), busy resets, no unhandled rejections into panel/hotkey callers. Edit PATCHes (`updateSlideItem`/`updateClipItem`) and delete commits (`removeClipItem`, video-page `handleDelete`/`bulkDelete`) now gate history pushes + UI refresh on `res.ok`; failed edit keeps its form open for retry; failed delete records nothing (no duplicate resurrection on undo). ENV GOTCHA: a FRESH `next dev` reads `.env.local`'s AUTH_SECRET (overrides `.env`) — forge local tokens from `.env.local` after any server restart (the old note about `.env` winning applied only to a stale long-running watcher). Also: if every route 404s with HTML after a dev-server kill/restart, the Turbopack cache is corrupt — `rm -rf .next` and restart.
  - **Undo coverage extended to the remaining destructive flows (implemented, browser-verified on localhost, NOT yet committed/deployed):** (1) Shared-folder page: collaborator annotation Delete now uses the same delayed-commit toast (`pendingDelete/pendingAnnotationIds` state + 8s timer + unmount commit; card hidden via new `liveAnnotations` memo filter that composes with the tag filter AND survives the 5s live-poll — poll replaces raw `annotations`, the filter applies at render); toast is hardcoded EN ("Deleted/Undo/Delete now") since the shared page doesn't use lib/translations. (2) Video page: delete saved summary replaced its confirm() with enqueueDelete — moments+panel hidden optimistically, revert restores both, commit sends DELETE /summarize. (3) Dashboard: Unshare-link ×, revoke-invitee, remove-saved-email and remove-video-from-folder all wrapped in enqueueDelete (no optimistic row hiding except saved-email chips which restore on undo; label keys `undo.shareLink/invite/savedEmail/removedFromFolder/summary` added EN+PT). E2E verified: shared-page delete→toast→Undo restores (server untouched), 8s auto-commit deletes, "Delete now" commits immediately, poll picks up externally-added annotations while one is pending; dashboard unshare→Undo keeps shareToken, no-undo path revokes after 8s; summary delete→Undo restores panel with server count intact. TEST GOTCHA (caused real damage): clicking an icon button by climbing ancestors from a folder-name span can land on the WHOLE sidebar and hit ANOTHER folder's Unshare — this revoked the user's real Uporto share token mid-test (re-shared immediately via POST /api/folders/[id]/share → NEW token, so old Uporto links died; user must redistribute). When targeting sidebar icon buttons, select `[title="Unshare"]` scoped to the exact row or use the LAST match for a freshly created test folder appended at the end of the list.
