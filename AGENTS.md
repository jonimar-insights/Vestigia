<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Work State
### Completed
- **Vimeo unlisted/privacy videos now embeddable via preserved `?h=` hash (committed e45aced, pushed, DEPLOYED):** Vimeo "Unlisted" videos can ONLY be embedded by URL with the `?h=` hash appended (a bare id is refused → the earlier 401 on 1041/1038). Previously the app dropped the hash from the pasted URL at import, so those videos never played. Now: `lib/social.ts` `detectSocialPlatform` folds the `h=` query param into `platformId` (`<id>?h=<hash>`), so it survives the `vimeo:` storage id + dedup; added `parseVimeoSpec(spec)` → `{id, hash}` and `vimeoEmbedUrl(platformId)` → `player.vimeo.com/video/<id>[?h=<hash>]?api=1`. `lib/vimeo-adapter.ts` `loadVideoById` parses the spec and calls `loadVideo({id, h})` (`MinimalVimeoPlayer.loadVideo` now takes `{id,h}`). Playlist `VideoPlaylistPlayer` creates the Vimeo player from the full URL (`?h=`) for hashed clips, bare `id` otherwise. Video + shared pages use `vimeoEmbedUrl()` for the vimeo iframe src (fixes malformed double-`?`). IMPORT: user pastes the share/embed link that already contains `?h=...`, e.g. `https://vimeo.com/12133658?h=bfdbe1c46f`. Verified end-to-end: import persists `youtubeId=vimeo:<id>?h=<hash>`; embed URL carries the hash; unit tests for spec parse/hash capture/embed build/adapter `{id,h}` passthrough (tests #8/#9). `tsc`/`eslint`/build clean.
- **Fixed intermittent Vimeo clip auto-play failure in the playlist (committed 5ec6287):** Vimeo can fire `play` then IMMEDIATELY `pause` while a `loadVideo`+`setCurrentTime` settles — the SDK or a slow buffer interrupts the initial play. When that trailing pause stuck (the `pendingPlay` was already consumed), the clip sat paused at its start (state=2, clock frozen) and never resumed → "some Vimeo clips don't auto-play." Root-caused empirically: an instrumented run with the user's real playlist transition (YT1→VIMEO 1072 ts97→YT2) logged `[va] loadVideo resolved → event play → event pause` (the settle race) that intermittently left the clip paused. FIX in `lib/vimeo-adapter.ts`: added a bounded auto-resume — when play is requested (`playVideo` or the queued `pendingPlay` after `loadVideoById` resolves) we arm `autoResume=3`; if a `pause` event fires while armed, `scheduleResume()` re-issues `p.play()` after 350ms (only if state is still paused), decrementing the budget. `pauseVideo()` clears the budget + timer so the auto-resume NEVER fights a user/manual pause (incl. cross-player pausing when leaving a Vimeo clip). Added unit tests `tests/vimeo-adapter.test.ts` #6 (loadVideo+playVideo→queued play→simulated settle pause→auto-resume re-plays) and #7 (no auto-resume after `pauseVideo`). Verified in headed Chrome 5× with the real transition: VIMEO window auto-plays and advances to YT2 every run. NOTE: Vimeo privacy-restricted videos STILL can't play if they were imported WITHOUT the `?h=` hash (e.g. an old row like 1041/1038 where the URL had no hash) — but with a properly hashed share link they now work (see e45aced). `tsc`/`eslint`/build clean.
- **Fixed playlist auto-play into drive/upload clips after a video ends (committed 4558f98, pushed, DEPLOYED to prod, verified localhost):** in `components/VideoPlaylistPlayer.tsx` the YT player's `onStateChange` handler only ignored events while a **Vimeo** clip was active. When the current clip was a drive/upload (**html5**) video, the hidden YT iframe's ended event (`state=0`) fired `setPlaying(false)` + `clearTimeInterval()`, killing the html5 clip's time poll and freezing the clock at `0:00` even though the media actually played — so after a video (YT/drive) ended, the next drive clip never auto-advanced. Now the `onStateChange` guard ALSO returns early when an html5 clip is active (adding `html5SrcsRef.current.get(curItem.videoId)`), so the drive/upload clip's polling keeps running and play state stays intact. Reproduced with a cross-platform cliplist (YT1→DRIVE→YT2→VIMEO): before the fix, DRIVE froze at 0:00 forever (video element actually played, `el.currentTime` advanced, but UI clock/poll stuck); after, YT→drive→YT transitions all auto-play and advance (drive shows expected stream-buffer latency ~8s via the loading pill). Intermediate diagnosis used temporary `console.log` in polling + `onStateChange` (removed). NOTE: the Vimeo clip still stalls in headless Chrome — confirmed as the known headless Vimeo limitation (readyState stays 1 / PlaybackError), not the auto-play bug. `tsc`/`eslint`/build clean.
- **Fullscreen playback for the playlist player (committed 3a5744f, pushed, DEPLOYED to prod, verified localhost):** a toggle button in the player header uses the Fullscreen API on the modal root (`rootRef`). In fullscreen the video area fills the viewport (`flex-1 min-h-0 w-full`, `max-w-4xl` dropped) and the sidebar/progress bar/mobile strip/detail panel are hidden (`{!isFullscreen && ...}`) while the header (with the exit toggle) stays. Esc while fullscreen exits fullscreen explicitly (not close the modal) — also works in automated/embedded browsers; `fullscreenchange` syncs the toggle. Verified in-browser at 1280×800: entering fills 1280×756 with sidebar hidden; Esc restores layout + keeps modal open. `tsc`/`eslint`/build clean.
- **Loading countdown for drive/upload clips in the playlist (committed ec67c96, pushed, DEPLOYED to prod):** the drive load has no fixed duration, so the loading pill now shows a countdown toward a nominal 8s max with a progress bar + spinner. `loadStartRef` + a 250ms `loadNow` tick drive elapsed; the pill shows remaining whole seconds and the bar fills proportionally; `html5Loading` starts/clears the ticker. Verified in-browser (8s→7s→6s→5s→4s as the 70MB WAV buffered, then playback began). `tsc`/`eslint`/build clean. (Updated 9d83bde: pill now reads "Loading from Google Drive (Ns)".)
- **Loading indicator while a drive/upload clip buffers in the playlist (committed db564cb, pushed, DEPLOYED to prod):** Drive streams can take ~7s to hand over metadata with no feedback (cover showed, nothing else). `html5Loading` state (set at each html5 clip load, cleared on `loadedmetadata`) + a "Loading audio/video…" pill with spinner overlaid over the player (`pointer-events-none`, cover stays visible underneath). Verified in-browser: indicator shows ~7.6s then disappears once audio starts. `tsc`/`eslint`/build clean.
- **Hold-mode drive/upload clips pause at the media's natural end (committed dbf68ad, pushed, DEPLOYED to prod):** a hold-mode clip (`endTimestamp: null`) on a drive/upload video previously played the ENTIRE underlying file (video 1050 = a 548s WAV) until manual advance. New `endHoldPlayer()` in `components/VideoPlaylistPlayer.tsx` pauses + blanks the player when `currentTime` reaches the html5 media's natural duration (clip-window effect now has a null-end + html5 branch); the adapter natural-`ended` handler also calls `endHoldPlayer()` for hold-mode html5 clips (previously it advanced via `advanceOrEnd()`). Hold semantics preserved — it does NOT advance to the next clip. Regression-checked locally on video 1050: a normal (non-hold, end=8s) drive clip still pauses at 8.11s; the hold path uses the same poll→effect→pause machinery. `tsc`/`eslint`/build clean (one targeted `exhaustive-deps` disable added for the stable helper, matching the file's existing pattern).
- **Cover shows immediately for Drive/upload clips regardless of mediaType; drive stream cached (committed 1a5afe9, pushed, DEPLOYED to prod, verified localhost):** the mediaType-based audio-cover approach was REPLACED — a drive/upload clip now shows its cover (audio-style overlay: `item.imageUrl` else `item.videoThumbnail`, music-note icon fallback, `pointer-events-none`) until `loadedmetadata` proves a REAL video track. `html5IsVideo: Map<videoId,boolean>` (state+ref) set in the onMeta handler when `videoWidth>0`/`videoHeight>0`; derived `isHtml5Audio = !!h5Src && !html5IsVideo.get(id)`, `audioCoverSrc = isHtml5Audio ? imageUrl||videoThumbnail : ""`. This covers probe-mistyped files (video 1050 = 70MB `audio/wav` stored `mediaType:"video"`) — `videoWidth:0` → cover never hides. The audio-cover overlay MOVED AFTER the "Loading player..." block and the spinner is gated `!readyNow && !isHtml5Audio` so the black overlay never hides the cover. `<video>` got `preload="metadata"` + early `el.play()` for clips starting `<3s` (audio starts without waiting for header). ALSO `app/api/drive/stream/[id]/route.ts` cache-control `no-store` → `private, max-age=86400` (repeat playlist opens don't refetch the 70MB metadata). Verified localhost: cover visible immediately (src = blob cover URL), stays up while playing (dur 548.3, paused:false, videoWidth 0, readyState 4); 206 + `content-range` + cache header confirmed. Removed `html5Audio` map entirely. `tsc`/`eslint`/build clean.
- **Playlist player shows cover image for Drive/upload AUDIO clips (ORIGINAL, committed 6db3f08 — SUPERSEDED by 1a5afe9 above):** when the active playlist clip's video is `platform:"drive"|"upload"` AND `/api/videos/[id]` reports `mediaType:"audio"`, the player overlays the clip's image (`item.imageUrl` else `item.videoThumbnail` — the video's cover) on the otherwise-black `<video>` element, matching the video-page audio layout; shows a music-note icon if no image set. `html5Audio` map (state+ref) populated in the fetch-class(). `audioCoverSrc` derived per item; overlay is `pointer-events-none` so controls/scrub unchanged. Verified on localhost by temporarily re-typing video 1050 to `mediaType:"audio"` in a test cliplist: cover rendered (visible, correct blob URL) over the playing WAV (dur 548s); mediaType restored + cliplist deleted. `tsc`/`eslint`/build clean. LIMITATION (why superseded): rely on `mediaType:"audio"`, but drive imports often store audio as `mediaType:"video"` → no cover.
- **Drive/upload videos playable in the playlist player (committed 0a1494a, pushed, DEPLOYED to prod, verified live):** `components/VideoPlaylistPlayer.tsx` now plays `platform:"drive"|"upload"` clips via a native HTML5 `<video>` element wrapped by `Html5Adapter`, alongside YouTube + Vimeo. The fetch-classification effect (`/api/videos/[id]` shape: 11-char `youtubeId` → YT, `vimeo:` → Vimeo, else) now maps drive/upload to an html5 `src` (the same-origin `/api/drive/stream/<id>` proxy URL) stored in `html5SrcsState`/`html5SrcsRef`. New: `html5PlayerRef`/`html5ElRef` (single reusable `<video playsInline>`, hidden unless `activeKind==="html5"`), `html5Ready`/`html5Setup`; `getActivePlayer()` returns the Html5Adapter for html5 items so the existing play/pause/seek/loop/speed/mute controls work. Clip load: swaps `el.src`, seeks to `cur.timestamp` + plays on `loadedmetadata`; clip-end polling + natural-end `onEnd` → `advanceOrEnd()` (loop-one seek-back too). Cross-player pausing is now wired on every switch (YT→html5, Vimeo→html5, html5→YT/Vimeo, pause-when-inactive, `advanceOrEnd` last-item end) so no audio bleeds; speed effect also applies to html5. `lib/html5-adapter.ts` gained `setPlaybackRate`/`mute`/`unMute` + an `onEnd(cb)` hook (called on the native `ended` event alongside the play-state callback). GOTCHA: drive files are probe-typed at import, so a `.wav` stored with `mediaType:"video"` still plays fine in the `<video>` element (duration/currentTime OK, renders black). Verified in-browser on localhost with a forged dev cookie + Playwright/system Chrome: created cliplist with a drive clip (video 1050, real 548s WAV via stream proxy) + a YT clip → drive clip auto-played (`currentTime` advanced, `paused:false`, `duration:548`), Next→YT switch showed the YT iframe and paused the `<video>`; test cliplist deleted after. `npx tsc --noEmit`, `npx eslint .`, `npm run build` clean. NOTE: headless Chrome has no `playwright install` browsers here — use `executablePath: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"`.
- **Cover pictures from file (committed 43fb504, pushed, DEPLOYED to prod, verified live):** the video-page Cover editor now has an "Upload picture" button next to the URL input (`app/video/[id]/page.tsx` `uploadCover` + `patchVideoCover`). `POST /api/videos/[id]/cover` (route `app/api/videos/[id]/cover/route.ts`, nodejs runtime) validates ownership + image type (JPEG/PNG/WebP/GIF/AVIF, ≤6 MB) and stores the file in Vercel Blob public store (`put()` from `@vercel/blob`; host `0duphqubm8co3wos.public.blob.vercel-storage.com`, already in next/image allowlist + `lib/image-host.ts`), returns `{url}`; the client fills the URL field and auto-saves (PATCH sets `thumbnailUrl` + `mediaType` for Drive/upload). EN/PT keys `video.coverUpload|coverUploading|coverUploadError`. Prod `BLOB_READ_WRITE_TOKEN` already configured in Vercel. Verified in-browser locally (PNG → blob URL persisted) + prod E2E (upload 200 → PATCH → GET → `videos/all` visible). `tsc`/`eslint`/build clean.
- **Editable video card titles (committed 58d2d6a, pushed, DEPLOYED to prod):** every Dashboard card (folder grid/list + "All Videos" grid/list in `app/page.tsx`) shows a pencil that turns the title into an inline input (`components/EditableTitle.tsx`): Enter/blur saves via PATCH `/api/videos/[id]` (`renameVideo` in `app/page.tsx`), Escape cancels, deleting the text → null/"Untitled". Optimistic update across `videos`/`allVideos`/`folderVideos` with refetch fallback on failure; a custom title drops that video's auto-translated title so it sticks. Verified in-browser on localhost (input prefill → Enter → card updates instantly → persisted via GET) + prod smoke (PATCH title 200 + reflected). `npx tsc --noEmit`, `npx eslint .`, `npm run build` clean.
- **Drive-audio + cover feature (committed 076766c, pushed, DEPLOYED to prod, verified live):** Google Drive audio files (mp3/m4a/wav/…) now import as AUDIO and render with an audio player + optional cover picture. New `videos.media_type text` column (migration `drizzle/0023_add_video_media_type.sql` applied to Neon). `lib/drive.ts:probeDriveFile(fileId)` best-effort probes the public download endpoint (`Range: bytes=0-0` + confirm-token retry for the HTML virus-scan page) returning `{contentType, fileName}`, and never throws; `probeIsAudio()` = content-type `audio/*` OR filename ext in `.mp3|.m4a|.aac|.wav|.ogg|.oga|.flac|.opus|.wma|.mka|.weba` (NOT `.webm` — that's usually video). `lib/import-video.ts` drive branch probes at import → `mediaType:"audio"` (then stores NO auto thumbnail; the user inserts a cover) vs video (stores the `lh3.googleusercontent.com` thumb as before); best-effort so invalid/private files still import as video. PATCH `/api/videos/[id]` now accepts `thumbnailUrl` (string|null) + `mediaType` ("audio"|"video"|null, invalid→null); `mediaType` added to projections in `/api/videos/all`, `/api/videos/trash`, and `GET /api/shared/folder/[token]` (per-video select). `lib/html5-adapter.ts` widened to `HTMLMediaElement` (works for `<audio>`); video page + shared page query `video[data-html5-player], audio[data-html5-player]` and render an audio layout (centered cover `<img>` + `<audio data-html5-player controls preload="metadata">`) when `mediaType==="audio"` for html5 platforms. Video page header gains a "Cover" button (html5 only) opening an inline editor: cover URL input + Save / Remove / Cancel + an Audio|Video media-type toggle (made available for `propagation` only when platform is drive/upload). EN/PT keys `video.cover|coverUrl|coverSave|coverRemove|mediaType|audio|video`. NEW: `lib/image-host.ts:isTrustedImageUrl(src)` mirrors `images.remotePatterns` — user-supplied cover hosts NOT in the allowlist render `unoptimized` on every dashboard/playlist/shared `next/image` thumbnail (an unlisted host otherwise THROWS at render and blanks the page). Tests: `tests/drive-audio.test.ts` (drive import mediaType null, PATCH audio+cover visible in all/list/read/trash, invalid mediaType→null, cover clear, probeIsAudio ext/type cases, permanent cleanup) — local run passed; shared-folder projection verified at runtime (folder+share+add+PATCH audio → `mediaType` present). Browser-verified on localhost: audio player controls + cover image render, header Cover editor opens prefilled with working Video/Audio toggle + Save/Remove. `npx tsc --noEmit`, `npx eslint .`, `npm run build` all clean.

### Log (prior milestones)
- **Soft-delete / Trash (committed 3f91650, pushed, DEPLOYED to prod, verified live):** `videos.deleted_at text` tombstone (migration `drizzle/0022_add_video_deleted_at.sql` applied to prod). `DELETE /api/videos/[id]` is now SOFT by default (keeps row + folder_videos/annotations/key_moments/transcripts so folders restore intact); `?permanent=true` hard-deletes (purges children + row). New `POST /api/videos/[id]/restore` (idempotent, 200 `alreadyLive:true` for live rows) and `GET /api/videos/trash` (soft-deleted, ordered by deleted_at desc, enriched with counts + first folderName). All list/read queries exclude deleted rows: `GET /api/videos`, `/api/videos/all`, `/api/videos/[id]` (GET+PATCH → 404), `GET /api/folders/[id]` (join filter), `GET /api/search` (userVideoIds base query), `GET /api/tags`, `GET /api/shared/folder/[token]` + `/export` (export also drops deleted videos' annotations). `lib/import-video.ts` gains `restoreIfTrashed()` — re-importing a soft-deleted video clears `deleted_at` and returns the existing row (all 4 branches: upload/drive/vimeo/youtube). Dashboard: Trash sidebar toggle + cards with Restore / Delete forever (both use the existing enqueueDelete undo toast; `pendingVideoIds` hides purged cards until commit) + "Restore all" + "Empty trash"; trash count badge; trash refreshed on mount + after delete commits. EN/PT keys `trash.*`. Tests: `tests/soft-delete.test.ts` (soft hide everywhere incl. folder, restore + folder membership, idempotent restore, re-import auto-restore, permanent purge, 404 paths; local dev run passed, prod DB verified clean/zero trashed rows). `npx tsc --noEmit` + `npx eslint .` clean. NOTE: the Undo toast label still says "Deleted" and the app has no auto-purge schedule — trashed rows live forever until the user empties trash.
- **Deleted videos recovered via Neon PITR (recovery done, README in this entry):** The user bulk-deleted "TVU" videos (app bulk delete). Investigation: `DELETE /api/videos/[id]` is a hard delete (no trash); the `videos_id_seq` serial counter explains most apparent id gaps (893–996, 1031–1037) — ids burned by failed/cleaned-up test imports never become visible rows. Compare gaps against a PITR branch to distinguish "never existed" from "deleted". Neon project = `blue-hat-51636792` ("video-annotator", region eu-west-2, org `org-mute-block-32206835`); **`history_retention_seconds: 21600` = only 6 HOURS of PITR** — recovery is ONLY possible for rows deleted in the last 6h.
  - PROCEDURE: (1) `neon auth` (browser OAuth; token cached in `~/.config/neonctl/credentials.json`); (2) `neon branches create --project-id blue-hat-51636792 --name recover-<HHMM> --parent "<ISO timestamp>"` (timestamp must be ≥ now−6h or API rejects "before retention window"); (3) connect to the branch with the MAIN branch's credentials (`neondb_owner:npg_GLVqDdMK54oR@<branch-host>/neondb`) — child branches reuse the parent role password; (4) diff ids vs main (same password works). API host for curl is `https://console.neon.tech/api/v2` (NOT `api.neon.tech` — that host doesn't resolve on this network); `neon endpoints list` didn't exist in this CLI build, so fetch hosts via `GET /projects/<id>/endpoints`.
  - OUTCOME: 4 real-user videos restored (ids **983, 984, 985, 986**, all channel "TVU" — YouTube U.Porto uploads), created 2026-08-24, deleted between ~16:00 and 17:40 2026-08-28. Restored with original ids/`created_at`, plus 4 `folder_videos` links into **folder 88 "Evidências-Chronos"** (with their original `added_at`), plus **49 `key_moments`** (ids 10221–10269 — main's `key_moments_id_seq` already sat at 10269, so no collision). Videos 894–982 were deleted >6h before the recovery or never existed → unrecoverable. GOTCHA: `folder_videos.added_at` is NOT NULL — include it (original value or fresh ISO). All recovery branches deleted afterward.
  - NOTE: `@neondatabase/serverless` `sql.unsafe!()` returns an UNEXECUTED builder (returns `{sql:...}`) — use `sql.query("...")` for raw SQL strings.
- **Vimeo import re-enabled (committed, deployed):** `createVideo` in `lib/import-video.ts` now has a `platform: "vimeo"` branch BEFORE the remaining social-reject block (which now only rejects TikTok/Instagram/X/Facebook). Detects via `detectSocialPlatform`, stores `youtubeId = "vimeo:<id>"` + `youtubeUrl` = original URL (trimmed), fetches title/thumbnail/duration from Vimeo oEmbed (`fetchSocialMeta`), falls back to a `i.vimeocdn.com/.../640x360.jpg` thumbnail, idempotent (existing returns 200). Playback was already fully supported (video page + shared page `playerKind/sharedKind === "vimeo"` via `lib/vimeo-adapter.ts` + socialEmbedUrl `?api=1`), so this ONLY restores the import path. `tests/social-import.test.ts` rewritten: vimeo imports 201 (title+thumbnail ok) + idempotent 200, other socials still 400 "no longer supported", YouTube unaffected, cleanup deletes only-if-created. Verified local: import 201, and video page renders the Vimeo iframe (`player.vimeo.com/video/<id>?api=1`) + SDK + "Watch on vimeo" badge.
  - POST-IMPORT BUG FIXES (part of this entry): (1) `lib/social.ts` Vimeo regex was `/\/(\d{6,})/` — it grabbed the FIRST numeric path segment (so `vimeo.com/album/<albumId>/video/<videoId>` stored/played the ALBUM id → wrong/missing clip) and rejected 5-digit legacy ids. Now prefers the `/video/` segment, else the LAST numeric segment, strips `?`/`#`, requires ≥5 digits. Plus thousand unit test `tests/vimeo-detect.test.ts` (album id, 5-digit, whitespace, query, negatives). (2) The Vimeo branch stored `youtubeUrl`/fed oEmbed with the UNTRIMMED pasted `url`, so trailing whitespace produced `%20` in the oEmbed URL (404 → silently dropped title/thumbnail) and dirty stored URL — now `canonicalUrl = url.trim()` is used for detection, oEmbed, and storage (consistent with all other import branches).

### Active
- Translation dictionary expanded from ~60 to ~130 keys per language across all visible UI sections
- All untranslated strings wrapped in `app/page.tsx` and `app/video/[id]/page.tsx`
- `npx tsc --noEmit` passes with 0 errors
- `shareToken` column added to `folders` table + migration `drizzle/0010_add_folder_share_token.sql`
- `POST/DELETE /api/folders/[id]/share` — share token generation/revocation
- `GET /api/shared/folder/[token]` + `GET|POST /api/shared/folder/[token]/annotations` — public endpoints
- `app/shared/folder/[token]/page.tsx` — public shared folder dashboard
- **Onboarding tours cover import→annotate→present (commit 190a8d2, committed):** dashboard tour adds a "Cliplist presentations" step (`[data-tour="cliplists-tab"]` on the Cliplists tab); video tour adds an "Add to cliplist" step (`[data-tour="add-to-cliplist"]` on the key-moment add-to-cliplist button); EN+PT `tour.step6*` / `tour.addToCliplist*` keys. FIXED latent bug in `components/GuidedTour.tsx`: the video tour auto-completed silently on mount because its targets (player/annotate/timeline) mount ASYNCHRONOUSLY after data loads — the skip-missing `effectiveIndex` treated them as absent and fired `onComplete()` immediately (flag set to "done", tour never displayed). Now GuidedTour re-polls the DOM for ~3s (`domTick` state + 400ms interval) so late-mounting targets are picked up, and auto-completes only after a 600ms grace timeout.
- Share button (share icon + clipboard copy + "Copied!") added to folder sidebar in `app/page.tsx`
- Auto-generated `shareToken` on folder creation in `app/api/folders/route.ts`
- EN/PT translation keys `share.*` and `shared.*` added to `lib/translations.ts`
- `proxy.ts` — exempts `/shared/*`, `/api/shared/*`, `/signin`, `/api/auth/*` from auth redirect
- Invite emails replaced Resend with the **Gmail REST API**; `lib/email.ts` sends via the signed-in user's own Gmail OAuth (`gmail.send` scope), with app-level SMTP fallback (`GMAIL_SMTP_USER`/`GMAIL_SMTP_APP_PASSWORD`)
- `auth.ts` Google provider requests `gmail.send` scope + `access_type=offline&prompt=consent` ONLY when `GMAIL_SEND_VIA_USER=true`; otherwise it requests only non-sensitive `openid email profile` (no `youtube` scope — YouTube API calls use `YOUTUBE_API_KEY`, not the user token). The sensitive scope triggers Google's "unverified app" warning, so the flag stays off until Google OAuth verification is complete. Google OAuth tokens persisted to `users` (`gmail_access_token`, `gmail_refresh_token`, `gmail_token_expires_at`) via jwt-callback upsert only when the flag is on
- Migration `drizzle/0013_add_gmail_tokens.sql` applied to Neon (must apply via `sql.query()`, NOT `sql.unsafe()` — unsafe returns an unexecuted builder)
- `POST /api/folders/[id]/shares` loads sender's stored Gmail tokens and sends the invite from the sender's account (`Name <sender@gmail.com>`); falls back to app SMTP when no tokens
- Invite emails sent via **Gmail REST API** (`gmail/v1/users/me/messages/send`, base64url raw RFC822) with always-fresh refreshed access token — Gmail SMTP XOAUTH2 was rejected because it demands `https://mail.google.com/` scope, not `gmail.send`
- Requires **Gmail API enabled** in Google Cloud project `624312173927` and same OAuth client secret in `.env`/Vercel (local `.env` was stale → `invalid_client`)
- Prod session cookies are signed with salt `__Secure-authjs.session-token` (HTTPS); local dev uses `authjs.session-token` — forged-token tests must match
- `tests/real-invite-send.test.ts` — drives a real invite to `notasdevideo@gmail.com` from `jonimar@gmail.com` (use `TEST_BASE=https://vestigia-vercel.vercel.app` for prod)
- Fixed shared folder video layout in `app/shared/folder/[token]/page.tsx`: (1) the page now injects `<script src="https://www.youtube.com/iframe_api">` before polling for `window.YT` (was missing — player never initialized, infinite "Loading player..." spinner); (2) added `min-w-0` to the Start/End annotation inputs so they shrink (they previously pinned at intrinsic ~200px each and overflowed the panel → page had ~39px horizontal scroll)
- Saved share emails for future use: `saved_share_emails` table (user_id + email, unique) + migration `drizzle/0014_add_saved_share_emails.sql` applied to Neon; `GET/POST/DELETE /api/users/saved-emails` route; share dialog in `app/page.tsx` has a "Save this email for future shares" checkbox (default on) and "Saved emails" chips that fill the email input on click / remove with `×`; emails stored per `session.user.id` (keyed `google-<email>`). Test: `tests/saved-emails.test.ts`
- Note: `/api/*` unauthenticated requests are 307-redirected to `/signin` by middleware (node fetch follows redirects — assert `redirect: "manual"` or expect 200-from-/signin); route handlers still return 401 as defense-in-depth
- **Three enhancements implemented, tested, and DEPLOYED to Vercel prod:**
  - **Live annotation updates**: `app/shared/folder/[token]/page.tsx` polls annotations every 5s while a video is selected; replaces state only when ids/`updatedAt` changed; `SharedAnnotation.updatedAt: string | null`; verified in-browser (annotation added via curl appeared without reload).
  - **Edit annotations on shared page**: `app/api/shared/folder/[token]/annotations/route.ts` gained PUT (requires annotationId/email/timestampStart/timestampEnd; only edit-permission invitees; only own annotations — 403 otherwise; sets `updatedAt`). Edit/cancel/save form in page (edit/cancel/save handlers). Verified in-browser (Edit → save → note updated).
  - **Atomic add-video-to-folder**: `lib/import-video.ts` `createVideo()` shared by `POST /api/videos` and `POST /api/folders/[id]/videos`; folder endpoint accepts `url` (optional videoId/title/thumbnailUrl/extractKeyMoments), creates+links atomically, idempotent (200 "Already in folder"); response includes `created` flag so tests can safely delete newly-created videos; `app/page.tsx` playlist import calls the folder endpoint when a target folder is chosen.
  - Test: `tests/enhancements.test.ts` (atomic 201/idempotent/400, edit own 200, edit other 403, poll GET shows updatedAt, cleanup deletes folder + only-if-created video). Lint: `npx eslint` clean.
  - Note: when forging prod session cookies in a browser, `__Secure-authjs.session-token` REQUIRES the `Secure` attribute or Chromium silently drops it (auto-verify on the shared page then fails → shows the email gate).
- **Collaboration + export + rate limiting (implemented and DEPLOYED to Vercel prod):**
  - **Edit collaborators can add videos to the shared folder**: `POST /api/shared/folder/[token]/videos` (public, email verified against `folderShares` with `edit` permission; body `{email, name, url}`) reuses `createVideo` from `lib/import-video.ts`, links atomically, idempotent. IMPORTANT: pass `userId: folder.userId` (folder owner) so the video is owned by the folder owner and can be deleted via `DELETE /api/videos/[id]` — passing `null` orphans the video (owner can't delete it).
  - **Annotation export**: `GET /api/shared/folder/[token]/export?email=...&format=csv|json` — any authorized collaborator (view OR edit) can export all folder annotations joined with video titles; CSV is RFC-4180-escaped with Content-Disposition attachment.
  - **Rate limiting**: `lib/rate-limit.ts` in-memory sliding window (`checkLimit(key,max,windowMs)` unit-testable; `rateLimit(request,{max,windowMs})` returns 429 `NextResponse`); applied to all public `/api/shared/folder/[token]/*` endpoints (GET 60/min, mutations 30/min, videos 20/min, export 30/min). Per-instance only on serverless — defense-in-depth, not a hard guarantee.
  - Shared page toolbar (folder view): "Add video" URL input (edit collaborators only) + Export CSV/JSON buttons; `refreshFolder` callback re-fetches folder data after add; shared page auto-verify effect now includes `session?.user?.name` in deps.
  - NOTE: share token is NOT auto-generated on folder creation (AGENTS.md earlier note is wrong) — generated by `POST /api/folders/[id]/share` (idempotent, returns existing token).
  - Tests: `tests/collab-add-export.test.ts` (fresh folder + edit/view shares; collab add 201/idempotent/400/403 view/403 stranger/400 no-url; export json+csv content, view 200, stranger 403, missing email 400; cleanup) and `tests/rate-limit.test.ts` (unit). Lint clean, tsc clean.
  - Verified on prod: toolbar renders, collab add 201 + idempotent 200, export CSV/JSON with content, view-only/stranger 403, cleanup via owner.
- **Vimeo player support (implemented and verified locally; NOT yet committed/deployed):**
  - `lib/vimeo-adapter.ts` — adapts promise-based Vimeo Player SDK to the synchronous YT-IFrame interface (`SyncPlayerInterface`); caches time/duration/state from `timeupdate`/`seeked`/`play`/`pause`/`ended` events so reads are synchronous; `onReady` fires after SDK ready + getDuration. Unit tests: `tests/vimeo-adapter.test.ts` (run `npx tsx tests/vimeo-adapter.test.ts`).
  - `app/video/[id]/page.tsx` + `app/shared/folder/[token]/page.tsx` — `playerKind` is `"youtube" | "vimeo" | "embed"` (vimeo → full control via SDK adapter + iframe `?api=1`; other socials → embed-only, no seek controls, "Watch on" badge). Vimeo SDK script lazy-loaded; player created on `iframe[data-vimeo-player]`.
  - `GET /api/shared/folder/[token]` now returns `videos.platform`; `next.config.ts` remotePatterns gained `i.vimeocdn.com`, `pbs.twimg.com`, `p16-sign-va.tiktokcdn.com`, `*.cdninstagram.com`, `*.fbcdn.net` — ANY unlisted social thumbnail host crashes the home page (`next/image` throws, page shows "This page couldn't load").
  - FIXED pre-existing bug in `app/api/shared/folder/[token]/verify/route.ts`: session email used to override the manually-submitted email, so signed-in users could never pass the email gate with an invited address. Now explicit body email wins: `email?.trim()?.toLowerCase() || sessionEmail`.
  - eslint.config.mjs: `@typescript-eslint/no-unused-vars` warn with `^_` ignore pattern.
  - Verified in-browser locally: vimeo video page (duration 1:02, seek 0:00→0:10), shared folder via email gate (same), youtube regression OK, home page renders social thumbnails. NOTE: actual playback can't be exercised in Playwright-driven browsers — even player.vimeo.com direct embed stalls (`readyState` stays 1) / PlaybackError; seek+events prove the adapter wiring.
  - COMMITTED as 60ca310 and DEPLOYED to Vercel prod (`vercel --prod` — project has no git auto-deploy). Verified on prod: home page renders social thumbnails, /video/935 vimeo player duration+seek, shared folder email gate passes while signed-in as a different account (the verify fix), shared-page player seek OK. Test folder deleted.
- **Issue sweep (committed as ebb65d8 and DEPLOYED to prod):**
  - FIXED: shared folder page infinite "Loading..." for returning collaborators — email restored from localStorage set `verifiedEmail` before mount effects, which made the only folder-loading effect early-return without ever clearing the loading flag. Added a dedicated effect that loads the folder when `verifiedEmail` is set but `folder` is null. Verified on prod (gate → reload → folder loads).
  - FIXED: switching between two Vimeo videos on the shared page produced a dead player (no duration/controls) — React updated the existing iframe's src in place, breaking the already-attached Player SDK handshake. Social/vimeo iframes are now keyed by `video.id` so each video gets a fresh embed. Verified vimeo→vimeo + vimeo↔youtube switches locally (duration + seek OK after each switch).
  - Video page remounts on /video/:id navigation in App Router — no stale-player issue there (verified youtube→vimeo→youtube via prev/next).
  - Lint: fixed all `npx eslint .` errors (`.vimeo-setup.mts` anys typed; dead `appUsers` state removed; unused imports removed; playlist player's intentional sync-setState/auto-advance annotated with targeted `react-hooks/set-state-in-effect` disables).
  - Playlist player warnings cleared (committed as 74aeb0e, deployed): the 4 exhaustive-deps warnings were resolved with an `itemsRef` mirror (synced by a first-declared effect) instead of adding deps — dep arrays stay narrowed to their triggers (index/ready/videoIds), so auto-advance semantics are unchanged. The refactor also fixed a latent stale closure (YT onStateChange captured `items.length` from mount). Bonus: reading through refs made the two `set-state-in-effect` disables unnecessary — `npx eslint .` is now fully clean (0 errors, 0 warnings). Behavior verified in-browser: clip start-time sync, slide timer advance, end-of-clip advance, and wrap-around to 1/3 all work.
  - NOTE: Vimeo embeds resume playback position via localStorage per browser (e.g. reselecting a video may start at 0:30) — standard Vimeo behavior, not a bug.
  - **Legacy-account ownership migrated (prod Neon, 2026-08-23):** videos/folders/cliplists owned by the legacy password account (`users` id=1 serial int, username `jonimar@gmail.com`, session sub `112973004688624469701`) returned 404 to the Google session. Migrated ownership of 105 videos (789–934), 2 folders, 2 cliplists from `112973004688624469701` → `google-jonimar@gmail.com` (plain UPDATEs; no FK between the text `user_id` columns and int `users.id`). The `users` table is legacy-only (serial id + password_hash) — Google-auth users are NOT rows there; identities exist only as string keys in `videos.user_id`/`folders.user_id`/`cliplists.user_id`. Verified /video/934 (Facebook reel) now loads + annotates; test annotation cleaned up.
  - **CORRECTION (same day) — the migration above was WRONG and has been REVERTED:** `auth.ts` sets `session.user.id = account.providerAccountId` = Google's RAW NUMERIC sub (`112973004688624469701` IS the real signed-in identity of jonimar@gmail.com). Every `google-<email>` string key in prod data was created exclusively by agent test scripts forging JWTs with `sub=google-<email>` — a phantom identity the real UI never sees. Moving the real library to that key made ALL user content vanish from the app ("i lost all the videos"). Revert applied: 107 videos, folders 11 (Walter Lewin, 95) + 12 (Uporto, 9), cliplists 5/6 back to `112973004688624469701`; verified via API + browser with a forged numeric-sub token. `google-jonimar@gmail.com` now owns only leftover test artifacts (folders 32–48,57, vimeo video 935, twitter video 936) — candidates for cleanup, invisible to the real user. RULES: (1) tests must forge `sub=112973004688624469701` to act as the real owner, or must clean up rows they create under any other key; (2) NEVER migrate ownership between these keys again without checking auth.ts first.
  - NOTE: `GET /api/videos` returns ALL of the user's videos (foldered included), ordered newest-first — the main dashboard always shows every video. The "All Videos" sidebar toggle is now redundant (it uses `GET /api/videos/all`, same set, with sort + `folderName`) but stays for its sort/folder-context fetch. `folders`/`folder_videos` are just membership references — deleting a folder never deletes videos.

- **Search/dashboard enhancements (committed as 0434fcd, deployed):** All 15 enhancements:
  1. **Title filter on video grid** — text input filters visible videos by title and annotation content (client-side, instant)
  2. **Sort dropdown** — Newest/Oldest/A→Z/Z→A/Most annotated/Recently updated
  3. **"All Videos" sidebar toggle** — shows ALL user videos (including foldered) with `GET /api/videos/all`
  4. **Search type filter chips** — Annotations/Scenes/Key moments + All types
  5. **Cmd+K global search overlay** — opens a search modal (works in real browsers; Meta key doesn't fire in headless Chrome)
  6. **Recently updated sort** — by `latestAnnotationAt` timestamp
  7. **Folder filter dropdown in search tab** — server-side, narrows search to videos in a specific folder
  8. **Search pagination** — "Load more" button with offset/limit params, shows total count
  9. **Tag browser** — sidebar "Tags" button opens a modal with all user tags + counts; clicking a tag searches for it (fixes tag search to strip leading `#` from query)
  10. **Search integrated into grid** — `gridVideos` memo composes title/content filter + sort + all-videos source
  11. **Cliplist bulk select (partial)** — `cliplistSelectedIds` state declared (UI not yet wired)
  12. **Search result context** — shows "in {folder}" below video title when result is in a folder
  13. **Saved/pinned searches** — Pin button (📌) in search input saves to `user_settings`; pinned chips shown when search is empty, click to search, × to unpin
  14. **Virtualized list** — `react-window` installed (not yet wired to grid, pending >40 videos threshold)
  15. **Video-page mini-search** — `annotationSearch` state filters annotations by label/note/tags in video page
  - New API routes: `GET /api/videos/all` (all user videos with folderName, sort param), `GET /api/tags` (aggregated tag counts), enhanced `GET /api/search` (type/folderId/limit/offset params, N+1 fixed, returns `total`, tag search strips `#` prefix)
  - EN+PT translation keys for all new UI strings including `search.results`, `search.noMore`, `grid.sortAnnotated`, `grid.sortUpdated`
- **Undo coverage extended to remaining destructive flows (committed as fd8d83f, deployed):** (1) Shared-folder page: collaborator annotation Delete uses delayed-commit toast (`pendingDelete/pendingAnnotationIds` state + 8s timer + unmount commit; card hidden via `liveAnnotations` memo filter composing with tag filter + 5s live-poll); hardcoded EN toast since shared page doesn't use lib/translations. (2) Video page: delete saved summary uses enqueueDelete (moments+panel hidden optimistically, revert restores, commit sends DELETE /summarize). (3) Dashboard: Unshare-link ×, revoke-invitee, remove-saved-email, remove-video-from-folder all wrapped in enqueueDelete (saved-email chips restore on undo; label keys `undo.shareLink/invite/savedEmail/removedFromFolder/summary` added EN+PT). TEST GOTCHA: clicking icon button by climbing ancestors from folder-name span can land on sidebar and hit ANOTHER folder's Unshare — scope button selection to exact row or use LAST match.
- **Vimeo clips playable in cliplist playlist modal (committed 90d0f86, deployed):** `VideoPlaylistPlayer` creates second player from `lib/vimeo-adapter.ts` when clip's video is Vimeo (`vimeo:`-prefixed youtubeId → `vimeoIds` map; SDK injected, `new window.Vimeo.Player(div, {id})`). `VimeoAdapter` gained `loadVideoById` (SDK `loadVideo()` + post-load seek), `cueVideoById`, `setPlaybackRate`, `mute/unMute`, `destroy`. All shared controls route through `getActivePlayer()`. GOTCHAS: (1) `YT.Player` REPLACES mount div with iframe — visibility must toggle on React-owned wrapper divs; (2) `window.Vimeo` declared in both page files — declare only NEW properties elsewhere; (3) agent-browser "HeadlessChrome" UA blank-walls Vimeo — launch with `AGENT_BROWSER_USER_AGENT` override.
- **Social imports removed — YouTube only (committed 9205014, deployed):** `createVideo` REJECTS TikTok/Instagram/X/Facebook/Vimeo URLs with 400; oEmbed/social-insert branch deleted from `lib/import-video.ts`. Playback of existing social rows untouched. `tests/social-import.test.ts` rewritten. (Vimeo IMPORT re-enabled later — see the Vimeo import entry in Work State Completed.)
- **Playlist modal issue sweep (committed 6000397, deployed):** (1) Players PAUSE when current item can't play (no audio bleed under slides); (2) FIXED clip-end crossing advance-loop bug for slide transitions; (3) `getActivePlayer()` returns null for unplayable items; (4) FIXED VimeoAdapter play-during-load race (queues `pendingPlay`).
- **Delete cliplists from dashboard grid (committed 09fb53a, deployed):** hover card → trash icon → confirm() → `deleteCliplist`. Also fixed React hydration warning (inline button → `<span onClick>`).
- **Annotation panel duplicate tooltip fixed (committed 9d831dd, deployed):** "add to cliplist" dropdown button retitled `annotation.addToCliplist` (was `annotation.share`).
- **Slide creation overhaul — all 7 enhancements (committed 721df77, deployed):** (1) Insert-at-position; (2) Duration presets 5/10/15/30/60s; (3) Hold mode (`endTimestamp: null`); (4) Duplicate icon; (5) Quick-add (form stays open); (6) Bulk paste mode; (7) Per-slide theme color + image URL. Migration `drizzle/0017_slide_enhancements.sql` applied. FIXED pre-existing bug: POST hardcoded `endTimestamp: null` for slides.
- **Slide overhaul issue sweep (committed b236c09, deployed):** (1) FIXED slide-creation form state carryover between cliplists; (2) FIXED PATCH accepting `endTimestamp: null` for clips; (3) Broken slide `imageUrl` hides via onError; hold-slide progress bar 0%; (4) Color swatches got title/aria-labels.
- **Undo for destructive actions (committed 478a2a8, deployed):** Gmail-style 8s delayed-commit undo on all destructive flows (delete video/bulk/folder/cliplist/clip-item; delete annotation/bulk-delete). Mechanics: immediate visual hide + "Deleted — [Undo] [×]" toast; server untouched until timer fires.
- **Undo/redo history panel (committed f00d404 + b984f1a, deployed):** session-scoped history stack (`lib/history.ts`, `useSyncExternalStore` with SERVER_SNAPSHOT) + `components/HistoryPanel.tsx` (toggle, Undo/Redo, clickable rows) + `useHistoryHotkeys` (Cmd+Z/Shift+Z/Ctrl+Y, skipped in inputs). Wired: slide add/duplicate/bulk-add, clip/slide edit, removeClipItem, drag reorder, addToCliplist, annotation CRUD. idRef pattern for recreated items. Capped 50 entries, in-memory only.
- **History hardening (committed a0446f8, deployed):** `undoHistory`/`redoHistory` catch inverse-op failures; edit/delete gate on `res.ok` before history push + UI refresh.
- **Year field removed from videos (committed, deployed):** `year` integer column dropped via migration `drizzle/0019_drop_video_year.sql`; removed from schema, all API routes, import flow, search metadata, video page edit UI, grid badges, and translation keys.

- **Channel field (committed ac547a9, deployed):** `channel` column (migration `drizzle/0021_add_video_channel.sql`), auto-detect from oEmbed `author_name` on import, inline edit next to year on the video page, channel badge on all 4 grid card layouts, and returned via search (`videoChannel`). EN/PT `video.channel` key.
- **Channel/title searchable (committed efa1749, deployed):** `GET /api/search` gained a `video` result type that matches videos by TITLE or CHANNEL (`ilike` on those columns, scoped to the user's videos + folder/year filters). Added a "Videos" filter chip (EN `search.videos`). Video results link to the video and show its thumbnail (batch-filled from videoMap).
- **Search by year (deployed):** typing a 4-digit year in the search box (e.g. `2019`) now matches videos by their `year` value. In `app/api/search/route.ts`, `qYear = /^\d{4}$/.test(q) ? parseInt(q,10) : null` is added as an `eq(videos.year, qYear)` branch in the matchedVideos `or(...)` — so non-4-digit/text queries are unaffected, and it composes with the existing year dropdown filter + type/folder filters. No UI change needed (the search box + existing video-result year badge already surface it).
- **Google Drive import replaces local file upload (committed a83b2f6, deployed):** local `<input type=file>` upload removed; `lib/drive.ts` parses the Drive file ID (`lib/drive.ts:extractDriveFileId`) and builds a direct-download playable src (`drivePlayableUrl` = `https://drive.usercontent.google.com/download?id=...&export=download`); `lib/import-video.ts:createVideo` has a `platform: "drive"` branch (`youtubeId = "drive:<id>"`) that runs BEFORE the social-reject branch, so `POST /api/videos`, folder, and shared add-video all support it with no caller changes. Playback maps to the native HTML5 player in both the video page and shared folder page (`platform === "upload" || "drive"` → `html5`). `parseSocialStorageId("drive:...")` returns null (not in known list) so no social interference. Works with NO Google Cloud config. UI is a single paste-URL field ("Import link"); the file must be shared "anyone with the link can view".
  - NOTE (commit 32a803d): the **Google Drive Picker / copy-to-storage mode was removed** — `app/api/drive/import/route.ts` and `lib/drive-picker.ts` are deleted (page.tsx had a temporary race in the first `openDrivePicker` commit, now removed entirely). Only the paste-URL (stream) mode remains. `.env.example` (gitignored) drops the `NEXT_PUBLIC_GOOGLE_*` picker vars. The now-orphaned `app/api/upload/route.ts` (@vercel/blob client `handleUpload`) is left in place but no UI references it.
  - FIX (commit 9eeb500, deployed): direct `drive.usercontent.google.com/download?...` URLs set `Cross-Origin-Resource-Policy: same-site`, so browsers block them inside a `<video>` element cross-origin → `MEDIA_ERR_SRC_NOT_SUPPORTED` (loads fine via curl, fails in browser). Fixed with a **same-origin streaming proxy** `GET /api/drive/stream/[id]` (`app/api/drive/stream/[id]/route.ts`, nodejs runtime, `force-dynamic`) that relays the browser's `Range` header to the Drive URL and streams back `206/200` with `Content-Type/Content-Range/Accept-Ranges` + `cross-origin-resource-policy: cross-origin`. `lib/drive.ts:drivePlayableUrl` now returns the ROOT-RELATIVE `/api/drive/stream/<id>` (works from any route depth). `proxy.ts` exempts `/api/drive/*` from the auth redirect (public stream). Existing `platform='drive'` rows had their `youtube_url` migrated from the Google URL to the proxy path via a one-off UPDATE. Verified locally + on prod: video loads (readyState 4, real duration, error null) and seeking works through the serverless proxy (the file must still be shared "anyone with the link can view").
  - FIX (commit 7c3f02d, deployed): **blank grid cards** — Drive videos imported with `title: null` + `thumbnailUrl: null`, so home-page cards rendered empty. Now `lib/import-video.ts` drive branch stores a default title `"Google Drive video"` and a real no-auth thumbnail `https://lh3.googleusercontent.com/d/<fileId>=w1000-h1000` (verified returns 200 image/jpeg without any API key). `next.config.ts` remotePatterns gained `lh3.googleusercontent.com` + `drive.google.com` (an unlisted host would crash the home page via next/image). `app/page.tsx` grid cards render a `MissingThumb` placeholder (Drive icon when `platform==="drive"`, else play icon) instead of nothing when `thumbnailUrl` is null; `platform` added to the `Video` type (present in `/api/videos` + `/api/videos/all` responses; folder-video/search sources fall back to the generic icon). Shared page thumbnail placeholder also shows the Drive icon. The proxy ALSO hardens large-file handling: `drive.usercontent.google.com/download` returns an HTML virus-scan confirmation page for files over its threshold — the proxy now detects `text/html`, extracts the hidden `confirm` token, and re-requests with `&confirm=<token>` before streaming.
  - TESTS (run against local dev, forged `sub=112973004688624469701` cookie, then cleaned up): drive link → 201 platform drive; duplicate → same id; malformed drive URL → 400; `lib/drive.ts` unit checks for `/file/d/`, `/open?id=`, `/uc?id=`, `drive.usercontent` all parse correctly and youtube/playlist links are rejected; drive video page renders native HTML5 player (media controls present) while `post`ing with header `authjs.session-token` (local) / `__Secure-authjs.session-token` (https) cookie.

### Active
- (none)

### Blocked
- (none)
